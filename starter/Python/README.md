Prerequisites:
* Python installed (latest stable)
* Python VSCode extension installed

```sh
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
# "Python: Select interpreter" in VSCode

# Run server
server

# Run client
client
```